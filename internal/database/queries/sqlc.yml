version: "2"
sql:
    - engine: "postgresql"
      queries: "query.sql"
      schema: "../migrations/00001_create_urls_table.sql"
      gen:
          go:
              package: "queries"
              out: "../queries"
              emit_prepared_queries: true